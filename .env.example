# FBR (on this middleware server)
FBR_BASE_URL=https://gw.fbr.gov.pk
FBR_BEARER_TOKEN=your-pral-issued-bearer-token

# Auth: app must send this in Authorization: Bearer <key>
MIDDLEWARE_API_KEY=your-secret-key-min-32-chars

# Optional
PORT=3001
