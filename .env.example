# Auth: invoicing app must send this in Authorization: Bearer <key>
# FBR token/URL are sent per request by the app (per company), not stored here.
MIDDLEWARE_API_KEY=your-secret-key-min-32-chars

# Optional
PORT=3001
